# Supabase Self-Hosted Config

# Dashboard User: admin / your-super-secret-password
POSTGRES_PASSWORD=your-super-secret-and-long-postgres-password

# JWT Secret (Use `openssl rand -base64 32` to generate)
JWT_SECRET=your-super-secret-jwt-token-at-least-32-chars-long

# API Keys (Use the JWT Secret to sign these tokens)
# Generate them here: https://jwt.io/
# Payload for ANON_KEY: { "role": "anon", "iss": "supabase", "iat": 1700000000, "exp": 2000000000 }
# Payload for SERVICE_ROLE_KEY: { "role": "service_role", "iss": "supabase", "iat": 1700000000, "exp": 2000000000 }
ANON_KEY=replace-with-your-anon-key
SERVICE_ROLE_KEY=replace-with-your-service-role-key

# URLs
SITE_URL=http://localhost:3000
SUPABASE_PUBLIC_URL=http://localhost:8000

# Realtime
REALTIME_ENC_KEY=your-super-secret-realtime-encryption-key
