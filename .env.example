# Supabase Configuration
# Update these with your local Supabase URL and Anon Key
# For local dev with the provided docker-compose:
# URL: http://localhost:8000 (Kong API Gateway) or http://api.crypto.local (via Ingress)
# Key: The 'anon' key from k8s/supabase/secrets.yaml (or the one you generated)

NEXT_PUBLIC_SUPABASE_URL=http://localhost:8000
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Site URL (used for Auth redirects)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
